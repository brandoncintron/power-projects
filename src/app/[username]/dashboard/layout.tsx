export default function UserDashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen">
      {/* User dashboard layout will be expanded later */}
      {children}
    </div>
  );
} 